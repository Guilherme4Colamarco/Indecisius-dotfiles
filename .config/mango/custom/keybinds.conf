# Custom key bindings — overrides pessoais
# Os binds do setup estão em configs/keybinds.conf
# Adicione aqui apenas overrides específicos que não pertencem ao default

bind=SUPER,h,spawn,~/.config/mango/scripts/wm/focus-or-cycle.sh left
bind=SUPER,l,spawn,~/.config/mango/scripts/wm/focus-or-cycle.sh right
